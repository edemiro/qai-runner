fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
websockets==14.1
python-dotenv==1.0.1

# Web target. After installing, fetch the browser once:
#   python -m playwright install chromium
playwright>=1.49

# Visual regression: decoding and diffing screenshots against a baseline.
Pillow>=11.0.0

# LLM providers — each on its own official SDK.
google-genai>=1.0.0
anthropic>=1.2.0
openai>=3.5.0

# Pulled in transitively by google-genai (via google-auth). From 49.0.0 on,
# cryptography publishes no macOS x86_64 wheel — only arm64 — so an Intel Mac
# silently falls back to building it from source and fails without a Rust
# toolchain. 48.x is the last release with a universal2 wheel. Capped only on
# that platform; everywhere else it stays current.
cryptography<49; sys_platform == "darwin" and platform_machine == "x86_64"

pytest==8.3.4
pytest-asyncio==0.25.0
